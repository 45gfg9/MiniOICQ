To run the MiniOICQ server, it's recommended to create a virtual environment first and then install the required packages.

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

Then run the server with:

```bash
python3 minioicq.py
```
